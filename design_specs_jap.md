# 自動議事録ベース生成ツール 設計仕様書

## 1. 目的および背景

本ツールは、プレゼンテーションソフトウェア（主にMicrosoft PowerPoint）を中心に業務が進行し、テキストベースのモダンな情報管理手法の導入が困難な環境において、現場の運用要件とシステム的なデータハンドリング要件を折衷するための次善策として設計されています。

対象となるPowerPoint資料から高精度なスライド画像とプレーンテキストを自動抽出し、罫線枠を備えたMicrosoft Word文書として再構築します。これにより、現場が要求する「スライドを見ながら隣の枠にメモを追記する」という視覚的な運用を維持しつつ、「テキストの検索性」「コピーの容易さ」「ファイルサイズの抑制」を確保します。

## 2. システムアーキテクチャおよび動作環境

処理は、NAS等からの資料取得、PowerPoint固有のレンダリング、再現性を求める文書処理、完成文書の返却に分離します。通常運用ではPowerShellランチャーが各段階を順に実行します。

### 2.1 Windowsホスト側

- **レンダリングエンジン:** Microsoft PowerPoint
- **起動方法:** Windows Script Host上のJScript
- **処理内容:** PPTX/PPTM等を同名PDFへ変換
- **出力場所:** 元のPowerPointファイルと同じディレクトリ
- **保持方針:** PDFは後続処理後も削除しない

PowerPoint自身のPDF出力を用いることで、元資料との描画差を抑えます。Windows側スクリプトは文書統合を担当せず、PDF生成だけを行います。

### 2.2 Podman/Dockerコンテナ側

- **ベースOS:** Debian Bookworm系Pythonコンテナ
- **画像処理:** Poppler、pdf2image、Pillow
- **Python主要依存の最低バージョン:** lxml 6.1.1、pdf2image 1.17.0、Pillow 12.3.0、python-docx 1.2.0、python-pptx 1.0.2、pip 26.2（これ以上を許容）
- **文書解析・生成:** python-pptx、python-docx
- **入力:** 元PowerPointファイルと、Windows側で生成済みの同名PDF
- **出力:** `統合議事録ベース.docx`

コンテナはPDFを生成または削除しません。PowerPointと同じstemを持つPDFを探索し、既存PDFからスライド画像を生成します。


### 2.3 PowerShellランチャー

- **入力:** NASまたはローカルの資料フォルダ
- **ローカル作業領域:** `%LOCALAPPDATA%\ppt2word\jobs` 配下の日時・PowerShell PID付きジョブフォルダ
- **コピー:** `robocopy` により入力フォルダ一式をローカルへ複製
- **処理制御:** JScriptによるPDF変換とPodmanコンテナを順次起動
- **出力返却:** 完成DOCXを指定フォルダへ一時ファイル経由でコピーし、`-Force`時は既存DOCXを一時バックアップへ退避してから置換
- **ログ:** ジョブ単位の `process.log`

PowerPointファイルは入力フォルダ直下のPPTX/PPTMを対象とします。相対リンク先などの補助ファイルを保持するため、サブディレクトリを含む資料フォルダ全体をローカルへコピーします。ただし、サブディレクトリ内のPowerPointファイルは自動処理対象に含めません。

入力側はコンテナへ読み取り専用でマウントし、出力側は別ディレクトリを読み書き可能でマウントします。失敗時はローカルジョブを常に保持します。正常終了時も既定では保持し、確認後の削除、または `-CleanupOnSuccess` による即時削除を選択できます。

## 3. 主要な設計と技術的解決策

### 3.1 Windowsレンダリングとコンテナ処理の分離

PowerPointとPDFの対応は、同一ディレクトリにある同名ファイルで管理します。

```text
example.pptx
example.pdf
```

コンテナ側は、処理開始前に全PowerPointファイルについて対応PDFの存在を検証します。これにより、処理途中で一部資料だけ欠落したWord文書が生成されることを防ぎます。

### 3.2 スライドの画像化と軽量化

- **PDF画像化:** Popplerを用いて既存PDFを既定300 dpiで1ページずつラスタライズし、全ページ画像をメモリへ同時保持しません。
- **16:9正規化:** 元画像をcropせず16:9キャンバス内へcontainし、4:3等では左右、21:9等では上下へ白余白を付与します。
- **ファイルサイズ抑制:** JPEG品質85を既定値とします。
- **中間ファイル:** JPEGは通常処理後に削除します。デバッグ時のみ保持できます。
- **PDF保持:** 生成PDFはローカルジョブ内に保持します。`-CleanupOnSuccess` を指定した正常終了時のみ、ジョブ削除とともに削除されます。
- **画像寸法:** Word上では高さ66 mmに固定します。

### 3.3 非表示スライドとPDFページの対応

PowerPointのPDF出力設定によって、PDFに全スライドが含まれる場合と、表示スライドだけが含まれる場合があります。Python側では次の両方を受け入れます。

1. PDFページ数がPowerPointの全スライド数と一致する場合
2. PDFページ数が表示スライド数と一致する場合

全スライドPDFの場合は、元PowerPoint XMLの非表示属性に基づき対応ページを除外します。見出しに表示するページ番号は元資料のスライド番号を保持します。

### 3.4 Wordドキュメントのレイアウト制御（空間隔離）

ユーザーによる予期しない編集でレイアウトが崩れることを防ぐため、1行2列の表を用います。

- **左セル:** スライド画像
- **右セル:** メモ入力領域
- **固定レイアウト:** `w:tblLayout w:type="fixed"` を直接設定
- **セル幅:** `w:tcW` をTwip単位で設定
- **画像セル:** 118.5 mm、左右・上下セル余白0.2 mm
- **記入セル:** A4本文幅170 mmから画像セル幅を差し引いた51.5 mm
- **罫線:** `w:tblBorders` を直接設定

### 3.5 抽出テキストの保持と隠蔽（w15:collapsed）

元PowerPointから `python-pptx` でテキストを抽出し、検索・コピー可能な通常文字としてWordへ保持します。通常テキストに加え、表セルおよびグループ内Shapeを再帰的に処理します。段落文字列を単位として取得し、文字装飾でrunが分割されていても不要な空白を挿入しません。視覚的なノイズを抑えるため、見出し段落へ次の属性を直接注入します。

```xml
<w15:collapsed w:val="1"/>
```

抽出テキストは画像レンダリング結果からOCRするのではなく、元PowerPointのテキストオブジェクトから取得します。

### 3.6 スタイルと段落間隔の統一制御

Normal、Title、Heading 1～3、Table Grid等のフォントを「メイリオ」に統一し、`w:rFonts w:eastAsia="メイリオ"` も設定します。

メイリオ使用時の不要な行高を避けるため、段落後の間隔を0 ptとし、同一スタイル間の追加間隔を抑制します。また、各段落および主要な段落スタイルへ `w:snapToGrid w:val="0"` を設定し、Wordの「文書グリッドが指定されている場合、行グリッド線に合わせる」を無効化します。

### 3.7 共同編集向け変更履歴

生成文書の `word/settings.xml` に `w:trackRevisions w:val="true"` を設定します。これにより、Wordで開いた時点から全ユーザーの編集を変更履歴として記録します。変更履歴のロックは行わず、通常の承諾・拒否操作を利用できます。

## 4. 出力ドキュメントの論理構造

1. **[Title]** 全体ヘッダー
2. **[Table]** 日時・参加者記入欄
3. **[Heading 1]** 資料ごとのヘッダー
4. **[Table]** 資料ごとの特記事項欄
5. **[Heading 2]** 元資料のスライド番号
6. **[Table]** 左セルの画像と右セルのメモ欄
7. **[Heading 3]** 抽出テキストの展開トグル
8. **[Normal]** 抽出テキスト

## 5. 実行単位

### 5.1 通常運用

```powershell
.\build_minutes.ps1 `
  -Source "\\nas\share\meeting-materials" `
  -Destination "\\nas\share\minutes"
```

`Destination`を省略した場合は入力元フォルダへ完成DOCXを返します。既定の出力名は `統合議事録ベース.docx` です。既存ファイルの上書きには `-Force`、正常終了時のローカルジョブ削除には `-CleanupOnSuccess` を明示します。

ランチャーは次を順に実行します。

1. 入力・出力・必要コマンド・Podmanイメージの検証
2. ローカルジョブフォルダの作成
3. 資料フォルダ一式のコピー
4. 対象PowerPointファイルの選択と同一stem衝突の検査
5. Windows PowerPointによる同名PDF生成
6. 全PDFの存在確認
7. 読み取り専用入力マウントと書き込み用出力マウントによるコンテナ実行
8. 完成DOCXの指定先への安全な返却（既存ファイルは新版の配置成功までバックアップ）

### 5.2 個別実行

Windows側PDF変換のみを行う場合:

```powershell
cscript.exe //nologo .\pptx_to_pdf.js .\meeting-a.pptx .\meeting-b.pptx
```

コンテナ処理のみを行う場合:

```powershell
podman run --rm `
  --mount "type=bind,source=$InputDir,target=/workspace,readonly" `
  --mount "type=bind,source=$OutputDir,target=/output" `
  ppt2word `
  -o "/output/統合議事録ベース.docx"
```

個別実行は障害切り分けやレイアウト調整時に用います。

## 6. 展望および拡張性

- 右セルの議事録テキストだけを再抽出してJSON化する機能
- Markdownの同時生成
- PDFの更新日時とPowerPointの更新日時を比較する事前検証
- Windows側PDF変換結果のmanifest出力
- ジョブ一覧表示および期限付きクリーンアップ
