# 自動議事録ベース生成ツール 設計仕様書

## 1. 目的および背景

本ツールは、プレゼンテーションソフトウェア（主にMicrosoft PowerPoint）を中心に業務が進行し、テキストベースのモダンな情報管理手法の導入が困難な環境において、現場の運用要件とシステム的なデータハンドリング要件を折衷するための次善策として設計されました。

対象となるPowerPoint資料から高精度なスライド画像とプレーンテキストを自動抽出し、罫線枠を備えたMicrosoft Word文書として再構築します。これにより、現場が要求する「スライドを見ながら隣の枠にメモを追記する」という視覚的な運用を維持しつつ、エンジニア側が要求する「テキストの検索性」「コピペの容易さ」「ファイルサイズの軽量化」を担保します。

## 2. システムアーキテクチャおよび動作環境

本ツールはホスト環境への依存を排除し、再現性を確保するため、コンテナ技術（Docker / Podman）を用いて実行環境をパッケージ化しています。

* **ベースOS:** Ubuntu (コンテナ内)
* **レンダリングエンジン:** ONLYOFFICE Document Builder
* 採用理由: LibreOffice等で生じるODF（Open Document Format）への変換プロセスを排除し、OOXMLのネイティブ処理によるピクセルパーフェクトな描画精度を確保するため。


* **画像処理:** Poppler, pdf2image, Pillow (Python)
* **ドキュメント生成:** python-docx, python-pptx (Python)
* **フォント管理:** ホストOS（Windows等）のフォントディレクトリを実行時にRead-Onlyでマウントし、`fc-cache`を更新することで、ライセンス上の問題を回避しつつ正確なフォントレンダリングを実現。

## 3. 主要な設計と技術的解決策

### 3.1. スライドの画像化と軽量化

* **16:9正規化:** 抽出されたスライド画像のアスペクト比を動的に評価し、4:3等の場合は左右に余白（パディング）を付与して16:9に正規化します。これにより、出力されるWord文書上での画像サイズを完全に統一します。
* **ファイルサイズ抑制:** 生成された画像はJPEG形式（品質設定85）で保存されます。これにより、数十枚のスライドを含む資料であってもファイルサイズの肥大化を防ぎます。

### 3.2. Wordドキュメントのレイアウト制御（空間隔離）

ユーザーによる予期せぬ編集（改行の連打やオブジェクトの移動）によるレイアウト崩壊を防ぐため、1行2列の表（Table）構造を採用しています。

* **左セル（画像・データ領域）と右セル（入力領域）の分離:** ユーザーの操作対象を右セルに限定することで、左セルに配置されたスライド画像および抽出テキストを保護します。
* **絶対幅・絶対高さの強制指定:** WordのAutoFit（自動幅調整）機能を無効化するため、内部XMLの `w:tblLayout w:type="fixed"` 属性を操作し、セル幅をTwip単位（dxa）で固定しています。また、行の高さもスライド画像の高さに合わせて `AT_LEAST` で確保し、均等なレイアウトを強制します。

### 3.3. 抽出テキストの保持と隠蔽（w15:collapsed ハック）

スライドから抽出したテキストデータは、検索性（Ctrl+F対応）を維持しつつ、視覚的なノイズにならないよう配置されます。

* **折りたたみ属性の注入:** 抽出テキストを特定の見出しレベル配下に配置し、Wordのアウトライン拡張属性である `<w15:collapsed w:val="1"/>` をXMLに直接注入します。これにより、ファイルを開いた初期状態においてテキスト領域が完全に折りたたまれて非表示となります。
* **可視文字としての保持:** 不自然な透明化や白文字化を行わず、グレーの可視文字として保持します。これにより、展開時の視認性を確保しつつ、Wordのスペルチェック（波線）によるユーザーの混乱を防ぎます。

### 3.4. スタイルの統一制御

* ドキュメント全体において、スタイルギャラリー（Normal, Heading 1～4, Table Grid等）のフォントを「メイリオ」に統一しています。EastAsiaフォント設定もXMLレベルで強制上書き（`<w:rFonts w:eastAsia="メイリオ"/>`）することで、日本語フォントの意図しないフォールバックを防止しています。

## 4. 出力ドキュメントの論理構造

ナビゲーションウィンドウでの視認性と、折りたたみ機能の干渉を防ぐため、以下の厳密なアウトライン階層に基づいてドキュメントを生成します。

1. **[Title]** 全体ヘッダー（統合議事録、日時、参加者など）
2. **[Heading 1]** ファイルごとのヘッダー（資料名、特記事項など）
* **[Heading 2]** スライドごとのアンカー（スライド番号）
* **[Table]** 左セル（画像） / 右セル（メモ入力枠）
* **[Heading 3]** 抽出テキストの展開トグル（表の直下に配置）
* **[Normal]** 抽出されたテキストデータ

この階層構造により、テキストの折りたたみ操作が上位のレイアウト（表や他の見出し）に波及することを論理的に遮断しています。

## 5. 展望および拡張性

本設計により生成されるWordドキュメントは、表構造と見出し階層が厳格に固定されています。この特性を活かし、将来的に以下の拡張が容易に実現可能です。

* **リバース・エンジニアリング（逆抽出）:** ユーザーが右セルに記入した議事録テキストのみをスクリプトで高精度に抽出し、JSON出力やチケット管理システム（Redmine等）への自動連携を行う機能。
* **Markdownの同時生成:** Wordファイルの生成と並行して、エンジニア向けにプレーンなMarkdown形式のドキュメントを同時出力する機能。
